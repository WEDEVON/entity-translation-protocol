# Encoding Incident Checklist

- stop broad manual edits
- verify whether the bytes are actually corrupted
- compare file content outside the terminal if needed
- restore from backup if corruption is real
- regenerate from clean artifacts if possible
- re-run audit before resuming normal work
